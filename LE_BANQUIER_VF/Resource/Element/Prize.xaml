<UserControl x:Class="LE_BANQUIER_VF.Resource.Element.Prize"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LE_BANQUIER_VF.Resource.Element"
             mc:Ignorable="d" ClipToBounds="True"
             FontFamily="Arial">
    <Grid>
        <!-- Background color changes based on IsEliminated -->
        <Border x:Name="PrizeBorder" BorderThickness="5" CornerRadius="10">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="Gold" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsAvailable}" Value="False">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation 
                                    Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" 
                                    To="LightGray" 
                                    Duration="0:0:0.5"
                                    BeginTime="0:0:3"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>


        <!-- Prize Amount -->
        <TextBlock Text="{Binding Amount, StringFormat=$\{0:0\}}" FontSize="20" FontWeight="ExtraBold"
           HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="DarkOliveGreen" />
    </Grid>
</UserControl>
